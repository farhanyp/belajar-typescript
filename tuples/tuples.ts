// Tuples
// Usage Tuples
let exampleTuples : [string, number, number]
exampleTuples = ["1", 2, 3]

let exampleTuples2 : [string, number, number] = ["1", 2, 3]

// Tuples with aliases
type User = [string, number]
let exampleTuples3: User = ["1", 2]

export{}
